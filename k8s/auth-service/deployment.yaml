apiVersion: apps/v1
kind: Deployment

metadata:
  name: auth-service
  namespace: safarsaathi

spec:
  replicas: 1

  selector:
    matchLabels:
      app: auth-service

  template:
    metadata:
      labels:
        app: auth-service

    spec:
      containers:
        - name: auth-service

          image: yash01er/safarsaathi-auth-service:1.0
          imagePullPolicy: Always

          ports:
            - containerPort: 9070

          env:
            - name: SPRING_PROFILES_ACTIVE
              value: docker

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9070
            initialDelaySeconds: 30
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9070
            initialDelaySeconds: 60
            periodSeconds: 20